'use strict';


/**
 * board 컬렉션 가져오기
 *
 * searchString String 검색을 위한 문자열 (optional)
 * sortKey Long 정렬을 위한 키 (optional)
 * no response value expected for this operation
 **/
exports.getUsers = function(searchString,sortKey) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

